import type { NextConfig } from "next";

// vinext reads next.config.ts during its build, so keep framework-compatible
// settings here even though the project is started with the vinext CLI.
const nextConfig: NextConfig = {};

export default nextConfig;
