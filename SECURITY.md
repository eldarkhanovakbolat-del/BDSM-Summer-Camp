# 安全与隐私

## 支持范围

当前版本面向单人自托管或彼此完全信任的环境，不包含多用户账号与租户隔离。

不要把默认实例直接开放给陌生人。需要公网访问时，先在站点外层增加可靠的登录或访问保护；若要服务互不信任的多人，则必须先实现真正的账号和数据所有权隔离。

## 不要提交的内容

- `.env` 与任何 API 密钥；
- 私人人设、关系文本、长期记忆；
- `data/` 中的数据库与缓存；
- 真实聊天记录和包含私人数据的日志；
- 私人服务器地址、内部拓扑或受限配置。

如果秘密曾进入 Git 历史，仅删除当前文件不够；应立即撤销并轮换密钥，再清理历史。

## 默认安全边界

- 浏览器通过同域 `/api` 使用服务端，不直接持有模型密钥；
- AI 和 Memory Provider 只读取服务端环境变量；
- 健康检查不返回上游地址或密钥；
- 观众调用与主 AI 记忆隔离；
- SQLite、`.env`、日志与人设文件被 `.gitignore` 排除。

## 已知限制

浏览器的 `visitor_id` 不是身份认证；进程内限流也不是公网防护。不要在没有额外安全设计时，将同一个实例开放给互不信任的多人。

## 报告问题

请优先通过 GitHub 的私密安全报告功能联系维护者。报告中不要粘贴真实密钥、完整私密对话或可直接利用的私人服务器信息；可以使用脱敏样例说明复现步骤。
