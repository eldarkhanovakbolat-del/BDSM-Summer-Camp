# 贡献指南

感谢你愿意一起完善 BDSM 夏令营。

## 适合首版的贡献

- 修复持续对话、恢复会话或移动端问题；
- 改进无障碍、性能与视觉细节；
- 增加中立、通用的 AI 或记忆 Provider；
- 更新可核对来源的 BDSM Wiki 目录；
- 改进部署文档和自动化测试。

账号系统、支付、通用 MCP、插件市场等较大范围功能，请先开 Issue 讨论，避免让核心体验和后端迅速膨胀。

## 提交前

1. 不包含密钥、私人人设、聊天数据库、私人域名/IP 或日志；
2. 不提交没有再分发许可的第三方完整文章；
3. 保持主 AI 名称由用户决定；
4. 保持五种参与方式、三栏目录、共学/体验和文字观众；
5. 保持主 AI 与观众记忆隔离；
6. 运行：

```bash
pnpm run build
node --test tests/rendered-html.test.mjs
python -m unittest -v server/test_app.py
python scripts/check_setup.py
```

## 新 Provider 的要求

- 只把特殊协议放在 `server/providers/`；
- 配置使用中立名称和服务端环境变量；
- 错误信息不得包含密钥、原始私密响应或记忆正文；
- 为正常、失败、主/观众隔离编写测试；
- 文档使用占位地址和假密钥。

提交贡献即表示你有权按本仓库许可证提供该内容。第三方内容仍应保留其原始许可证和署名。
